import { DateconvdayPipe } from './dateconvday.pipe';

describe('DateconvdayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateconvdayPipe();
    expect(pipe).toBeTruthy();
  });
});
