import { LabFilterPipe } from './lab-filter.pipe';

describe('LabFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LabFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
