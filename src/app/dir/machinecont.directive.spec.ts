import { MachinecontDirective } from './machinecont.directive';

describe('MachinecontDirective', () => {
  it('should create an instance', () => {
    const directive = new MachinecontDirective();
    expect(directive).toBeTruthy();
  });
});
