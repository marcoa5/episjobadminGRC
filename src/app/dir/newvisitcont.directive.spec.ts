import { NewvisitcontDirective } from './newvisitcont.directive';

describe('NewvisitcontDirective', () => {
  it('should create an instance', () => {
    const directive = new NewvisitcontDirective();
    expect(directive).toBeTruthy();
  });
});
