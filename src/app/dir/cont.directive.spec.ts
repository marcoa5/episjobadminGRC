import { ContDirective } from './cont.directive';

describe('ContDirective', () => {
  it('should create an instance', () => {
    const directive = new ContDirective();
    expect(directive).toBeTruthy();
  });
});
