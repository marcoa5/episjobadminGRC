import { TablecontDirective } from './tablecont.directive';

describe('TablecontDirective', () => {
  it('should create an instance', () => {
    const directive = new TablecontDirective();
    expect(directive).toBeTruthy();
  });
});
