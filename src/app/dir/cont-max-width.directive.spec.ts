import { ContMaxWidthDirective } from './cont-max-width.directive';

describe('ContMaxWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ContMaxWidthDirective();
    expect(directive).toBeTruthy();
  });
});
