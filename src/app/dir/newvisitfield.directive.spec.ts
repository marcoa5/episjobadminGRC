import { NewvisitfieldDirective } from './newvisitfield.directive';

describe('NewvisitfieldDirective', () => {
  it('should create an instance', () => {
    const directive = new NewvisitfieldDirective();
    expect(directive).toBeTruthy();
  });
});
