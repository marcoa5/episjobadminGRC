import { NewvisitspacerDirective } from './newvisitspacer.directive';

describe('NewvisitspacerDirective', () => {
  it('should create an instance', () => {
    const directive = new NewvisitspacerDirective();
    expect(directive).toBeTruthy();
  });
});
