import { NewvisitsubcontDirective } from './newvisitsubcont.directive';

describe('NewvisitsubcontDirective', () => {
  it('should create an instance', () => {
    const directive = new NewvisitsubcontDirective();
    expect(directive).toBeTruthy();
  });
});
