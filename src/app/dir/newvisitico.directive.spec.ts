import { NewvisiticoDirective } from './newvisitico.directive';

describe('NewvisiticoDirective', () => {
  it('should create an instance', () => {
    const directive = new NewvisiticoDirective();
    expect(directive).toBeTruthy();
  });
});
